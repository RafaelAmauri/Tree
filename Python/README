Usage: python3 tree.py [-h] [-d D] [-maxdepth MAXDEPTH]

This python script opens a given directory and lists its contents in a tree-like fashion. The idea behind this project
is to avoid using recursion for the implementation and being as memory-efficient as possible.